spring:
  datasource:
    url: jdbc:sqlite:./target/test-restaurant.db
    driver-class-name: org.sqlite.JDBC
  flyway:
    enabled: true
    locations: classpath:db/migration-sqlite
    baseline-on-migrate: true
